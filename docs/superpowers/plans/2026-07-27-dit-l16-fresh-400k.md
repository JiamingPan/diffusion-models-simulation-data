# Fresh DiT-L16 400k Sweep Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the not-yet-run fresh 300k workflow with an isolated ten-size DiT-L16 sweep that trains to 400k and evaluates at 200k, 300k, and 400k.

**Architecture:** Rename the sweep-specific files and roots from `fresh300k` to `fresh400k`, expand the frozen manifest from twelve to sixteen 25k stages, and gate only the three approved scientific milestones with exact sampling and PCA/SSCD audits. Update the notebook so 400k is final and 300k is intermediate.

**Tech Stack:** Python 3.10, PyTorch, diffusers, Slurm, NumPy, pandas, matplotlib, Jupyter, pytest.

## Global Constraints

- Train all ten sizes \(2^6\) through \(2^{15}\) from fresh seed-123 initialization.
- Target 400,000 requested optimizer updates in sixteen 25k stages.
- Evaluate only 200k, 300k, and 400k.
- Never use `fresh300k` or legacy continuation artifacts.
- Keep at most two GPU tasks active.
- Do not force or tune for a rightward transition.

---

### Task 1: Lock The 400k Manifest Contract

**Files:**
- Rename: `scripts/prepare_nf_generalize_fig2_dit_l16_fresh300k_configs.py` to `scripts/prepare_nf_generalize_fig2_dit_l16_fresh400k_configs.py`
- Rename: `tests/test_nf_fig2_dit_l16_fresh300k.py` to `tests/test_nf_fig2_dit_l16_fresh400k.py`

- [x] Change tests first to require 160 rows, sixteen stages, a 400k final target, scientific milestones `{200000, 300000, 400000}`, and `fresh400k` roots.
- [x] Run `pytest -q tests/test_nf_fig2_dit_l16_fresh400k.py` and confirm it fails against the 300k implementation.
- [x] Rename and minimally update the preparer, run names, manifest version, roots, labels, and validation.
- [x] Rerun the focused test and confirm it passes.

### Task 2: Extend The Great Lakes Workflow

**Files:**
- Rename all `scripts/*fresh300k*` workflow and audit files to `fresh400k`.
- Modify the renamed submit, train, sample, precheck, and audit scripts.

- [x] Add failing source-contract tests for sixteen stages, 400k completion, three scientific milestones, exact audit gating, and no `fresh300k` paths.
- [x] Run the focused workflow tests and confirm the expected failures.
- [x] Update the submit chain to stages 1 through 16 and submit sampling/analysis only at stages 8, 12, and 16.
- [x] Update precheck, train, sample, audit paths and accepted update choices to `fresh400k`.
- [x] Run shell parsing, Python compilation, and focused tests.

### Task 3: Make 400k The Notebook Final Result

**Files:**
- Rename: `scripts/update_dit_fresh300k_notebook.py` to `scripts/update_dit_fresh400k_notebook.py`
- Rename: `tests/test_dit_fresh300k_notebook.py` to `tests/test_dit_fresh400k_notebook.py`
- Modify: `notebooks/nf_generalize_fig2_dit_results.ipynb`

- [x] Change notebook tests first to require 200k equal-budget, 300k intermediate, 400k final, full/zoom figures, and complete ten-size PCA/SSCD audits.
- [x] Run notebook tests and confirm failure.
- [x] Update the notebook generator and regenerate the notebook.
- [x] Compile every notebook code cell and verify updater idempotence.
- [x] Run notebook presentation tests.

### Task 4: Documentation, Verification, And Integration

**Files:**
- Modify: `README.md`
- Modify: `scripts/slurm/README.md`

- [x] Replace the authoritative fresh 300k documentation with fresh 400k instructions.
- [x] Run `pytest -q`.
- [x] Run `git diff --check`, shell parsing, Python compilation, and notebook compilation.
